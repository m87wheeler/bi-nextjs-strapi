import type { NextPage } from "next";
import CardCarousel from "../components/card-carousel/card-carousel";

const Home: NextPage = () => {
  return (
    <div>
      <CardCarousel />
    </div>
  );
};

export default Home;
